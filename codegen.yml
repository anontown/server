overwrite: true
schema: "resources/app.gql"
documents: null
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript-common"
      - "typescript-server"
      - "typescript-resolvers"
config:
    scalars:
      DateTime: string
